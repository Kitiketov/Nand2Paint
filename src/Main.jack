class Main
{
    static Cursor cursor;
    static PaintTools paintTools;
    static bool startDrawing;
    static bool keyFlag;
    static bool isRelize;

    function int binPow(int a, int n){
        var int p;
        if (n=0){
            return 1;
        }
        if (n/2*2*-1+n=1){
            let p = Main.binPow(a,n-1)*a;
            return p;
        }
        else{
            let p = Main.binPow(a,n/2);
            let p = p * p;
            return p;
        }
    }


    function void main()
   {
        var int key, tick;

        // do Screen.setColor(true);
		// do Screen.drawPixel(15, 1);
        // do Screen.setColor(Cursor.getColor(15,1));
        // do Screen.drawPixel(16,16);

        // do Screen.setColor(true);
		// do Screen.drawPixel(0, 0);
        // do Screen.setColor(Cursor.getColor(0,0));
        // do Screen.drawPixel(16,16);
        // return;

        do DrawUI.DrawUI();
        do Cursor.init();
        do DrawUI.updatePosition();        
        do paintTools.init();
        let keyFlag = true;

        while (true){
            if (tick = 2000){
            let key = Keyboard.keyPressed();
            if (startDrawing ){
                if (Cursor.prevIsNone()){
                    do Cursor.savePosition();
                }
            }
            if (key = 0 ) {let keyFlag = true;}
            if (key = 32 & keyFlag) {let startDrawing = ~startDrawing; let keyFlag = false;}
            if (key = 87) {do Cursor.moveDirection(Vector.new(0,-1));}
            if (key = 65) {do Cursor.moveDirection(Vector.new(-1,0));}
            if (key = 83) {do Cursor.moveDirection(Vector.new(0,1));}
            if (key = 68) {do Cursor.moveDirection(Vector.new(1,0));}
            if (key = 49 & keyFlag) {
                do PaintTools.changeColor();
                let keyFlag = false;
                do DrawUI.updatePosition();
            }
            if (key = 50) {do PaintTools.setInstrument(2); do DrawUI.updatePosition();}
            if (key = 51) {do PaintTools.setInstrument(3); do DrawUI.updatePosition();}
            if (key = 52) {do PaintTools.setInstrument(4); do DrawUI.updatePosition();}
            if (key = 53) {do PaintTools.setInstrument(5); do DrawUI.updatePosition();}
            if (key = 54) {do PaintTools.setInstrument(6); do DrawUI.updatePosition();}
            if (key = 55) {do PaintTools.setInstrument(7); do DrawUI.updatePosition();}
            if (key = 90 & keyFlag) {do PaintTools.IncSize(); let keyFlag = false; do DrawUI.updatePosition();}
            if (key = 88 & keyFlag) {do PaintTools.DecSize(); let keyFlag = false; do DrawUI.updatePosition();}
            if (key = 69 & keyFlag) {do PaintTools.tryRelize(); let keyFlag = false; let startDrawing = false; do DrawUI.updatePosition();}
            let tick = 0;
            }
            if (startDrawing = true) {do PaintTools.tryDraw();}
            let tick = tick + 1;
        }
        return;
   }
}
