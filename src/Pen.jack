class Pen{
      constructor Pen new() {
        return this;
        }
        method void drawPixel(int color,int x, int y){
            do Screen.setColor(color);
            do Screen.drawPixel(x,y);
            return;
        }
}